<?php

namespace Tests\Feature\Invoices;

use Illuminate\Foundation\Testing\RefreshDatabase;
use Tests\TestCase;

class StoreTest extends TestCase
{
//     use RefreshDatabase;

//     public function setUp(): void
//    {
//        parent::setUp();
//
//        $this->artisan('db:seed');
//    }

    public function testItStoresANewInvoice(): void
    {
//        $response = $this->postJson('invoices', [
//            'user_id' => 1,
//            'quantity_products' => 7,
//            'total' => 100000,
//            'status' => 'PENDING'
//        ]);
//        $this->assertDatabaseHas(
//            'invoices',
//            ['cart' => 1, 'quantity_products' => 7, 'total' => 100000, 'status' => 'PENDING']
//        );
//        $response->assertOk();

 //      $invoice = new invoice();
//        $invoice->user_id = auth()->user()->id;
//        $invoice->user_id = 1;
//        $invoice->reference = (random_int(1000000000, 10000000000));
//        $invoice->total = $request->input('total');
//        $invoice->status = 'PENDING';

//        $invoice->save();
//        return redirect(route('invoices.index', $invoice));

//        $invoice = new invoice();
//        $invoice->user_id = auth()->user()->id;
//        $invoice->reference = (random_int(1000000000, 10000000000));
//        $invoice->total = $request->input('total');
//        $invoice->status = $request->input('status');
    }
}
